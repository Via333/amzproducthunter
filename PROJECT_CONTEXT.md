# AMZ 选品自动化项目上下文

这份文档用于每次重新开始这个项目时先阅读，避免忘记之前已经确定的逻辑、进展和注意事项。

## 项目目标

搭建一个面向个人卖家的 Amazon US 选品自动化系统。

系统目标不是简单找高销量产品，而是找：

- 有一定稳定需求
- 竞争不要过强
- 评论门槛不要太高
- 有利润空间
- 适合个人卖家切入
- 供应链、体积、合规、售后风险可控
- 后续值得做单品深度研究的机会产品

最终希望把结果集中展示在网页里，而不是依赖多个 CSV 或 Excel 文件来回查看。

## 当前核心流程

目前项目逻辑已经从“单个 ASIN/关键词出发”调整为更清晰的多层验证流程：

1. 选品扫描
   - 使用 Sorftime CLI / 已有脚本获取数据。
   - 默认不直接爬 Amazon 前台。
   - 以 Amazon US 市场为主。
   - 扫描重点应偏向最小层级类目，而不是只依赖用户提供关键词。
   - 每周优先扫描从未扫描的类目；全部覆盖后按最久未扫描时间轮换。
   - 类目状态写入 `archive/category_scan_state.csv`，避免每周重复扫描同一批类目。
   - 明确不适合的类目写入 `config/category_exclusions.json`，以后永久跳过并保留原因。

2. 关键词选品入口
   - 当用户已经有方向时，运行 `python3 keyword_opportunity_search.py --keyword "关键词"`。
   - 每次最多读取 100 个相关产品，使用与类目发现相同的个人卖家过滤和评分标准。
   - 搜索结果独立归档到 `archive/keyword_search_runs/`，索引保存在 `archive/keyword_search_index.csv`。
   - 关键词结果只是候选，仍需经过最小类目/形态验证才能进入正式机会池。

3. 候选产品发现
   - 从多个类目/策略中发现候选 ASIN。
   - 候选只是入口，不等于机会。
   - 高销量不直接等于可做。

4. 类目/形态验证
   - 每个候选 ASIN 需要进入它所在的最小类目。
   - 拉取或分析该类目的 Top 产品。
   - 把 Top 产品拆成不同产品形态。
   - 判断机会对象应该是“类目里的某个形态”，而不是孤立的单个 listing。

5. 机会池
   - 只有通过类目/形态验证的产品或形态，才进入机会池。
   - 同一类目、相似形态要合并展示，避免重复研究。
   - 历史机会必须归档，不能每次更新后覆盖掉旧机会。

6. 单品深度研究
   - 用户可以从机会池中指定某个机会做深度研究。
   - 已经研究过的单品结果需要存档，方便以后继续查看。

7. 网页展示
   - 所有核心结果都应集中展示在 `web/index.html`。
   - 页面要让用户能直接看懂：为什么进入机会池，为什么被淘汰，下一步该做什么。

## 选品判断原则

这个项目面向个人卖家，所以筛选标准要偏保守。

优先考虑：

- 中小件
- 非强品牌垄断
- 非强合规
- 非高破损
- 非高售后
- 有一定销量但不是超级红海
- 评论数不过高
- 有低评论高销量样本
- 有材料、功能、套装、尺寸、图案、配件等差异化空间
- 供应链有可能找到

明确排除或谨慎：

- Dyson、OXO、KitchenAid、Made In、VEVOR 等强品牌或官方品牌护城河明显的产品
- 电子核心件
- 成人用品
- 医疗/健康强合规产品
- 食品、入口类、强认证类产品
- 大件、重货、家具、地毯等物流风险高产品
- 易碎、高破损、高退货产品
- 强季节性或极窄使用场景产品
- 明显被头部品牌垄断的类目
- Top 产品评论墙太高的类目
- 已明确标记为大件、强专利/品牌垄断、强认证或高产品责任的类目

## 评分和筛选重点

评分不是只针对单个 listing，而要逐步升级为针对“类目里的产品形态”。

需要重点看：

- 月销量是否达到稳定需求
- 评论中位数是否过高
- Top10 是否形成评论墙
- 低评论高销量产品数量
- 头部品牌占比
- CN/HK 卖家占比
- FBA 占比
- 价格带
- 预估毛利率
- 产品体积和重量
- 变体数量
- 是否存在评分缺口
- 是否存在评论痛点
- 是否有明确差异化切入口

对个人卖家来说，不追求最高销量，更重视：

- 单品利润
- 小而稳定的需求
- 低竞争
- 可差异化
- 可供应
- 风险可控

## 单品深度研究要求

单品研究不能只输出一句简单机会结论。必须围绕产品形态和用户反馈展开。

每个单品研究至少应覆盖：

- 产品形态分类
- 每种形态的示例图片
- 材质
- 颜色
- 套装数量
- 功能差异
- 尺寸差异
- 配件或附加元素
- 包装/组合方式
- 图片视觉风格
- 评论中的消费者在意点
- 评论中的不满意点
- 当前竞品没有解决的痛点
- 哪些功能或规格供给少但需求可见
- 哪些图案、颜色、套装或场景可能有微创新空间
- 是否值得找供应商验证
- 是否值得打样
- 是否暂缓

产品形态下面要直接展示对应的图片、特征和评论分析，避免用户还要到页面下面单独找图。

## 评论分析要求

评论分析应尽量基于真实评论数据。

优先使用 Sorftime 官方 CLI / API 能拿到的评论接口。

评论分析需要分层：

- 正面关注点：用户为什么买、满意什么
- 负面痛点：用户吐槽什么
- 高频问题：反复出现的问题
- 未满足需求：用户想要但当前产品没有很好满足的点
- 产品改造方向：能否通过材质、尺寸、结构、配件、包装、图案、说明书等解决

不要只写泛泛结论，例如“可以优化材质”。需要尽量具体到：

- 哪个材质问题
- 哪种尺寸问题
- 哪个使用场景问题
- 哪个结构问题
- 哪个包装或配件问题

## 网页可读性要求

网页是这个项目的主要使用入口。

页面应清晰展示：

- 总览
- 选品挖掘
- 类目/形态验证
- 机会池
- 单品研究档案
- 生成逻辑
- 下一步

可读性原则：

- 不要把同类产品重复打散
- 同一类目或相似形态要分组
- 不要把“相邻形态证据”误放进机会池
- 被淘汰的产品要说明淘汰原因
- 进入机会池的产品要说明进入原因
- 单品研究里，产品形态、图片、特征、评论和机会结论要放在一起
- 按用户决策顺序组织页面，而不是按脚本输出顺序堆数据

## 定期更新要求

项目需要每周更新一次即可。

每周更新目标：

- 扫描约 100 个最小层级类目
- 每个类目最多看 100 个 Top 产品
- 优先覆盖未扫描类目，再按最久未扫轮换
- 永久跳过已记录的不适合类目
- 发现新候选产品
- 做类目/形态验证
- 更新机会池
- 保留历史机会
- 重新生成网页

每周更新后应输出：

- 扫描了多少类目
- 新增候选数
- 通过验证进入机会池数量
- 被淘汰的主要原因
- 需要人工复核的产品
- 网页是否已更新

## 当前重要约束

- 不要直接爬 Amazon 前台。
- 优先使用 Sorftime CLI / API 和已有脚本。
- 不要把 API key、account-sk、token、cookie 写入文档或输出。
- 不要删除文件。
- 不要批量删除文件或目录。
- 如确实需要删除大量文件，必须停止并让用户手动处理。

## 已知重要脚本和文件

以下是项目中已经使用过或约定过的入口，开始工作时应优先检查是否存在和当前实现是否一致：

- `discover_sorftime_opportunities.py`
  - 负责使用 Sorftime 数据做选品发现。
  - 当前方向应偏向类目扫描，而不是只靠关键词。
  - 使用 `archive/category_scan_state.csv` 做持久化轮换。

- `keyword_opportunity_search.py`
  - 负责用户指定关键词的相关产品搜索、统一评分、历史归档和网页刷新。

- `category_shape_validation.py`
  - 负责类目/形态验证。
  - 核心是判断候选产品所在类目里的具体形态是否有机会。

- `refresh_selection_workflow.py`
  - 每周更新主入口。
  - 应串起选品发现、类目/形态验证、机会池更新和网页生成。

- `refresh_product_research.py`
  - 单品深度研究入口。
  - 使用方式应类似：`python3 refresh_product_research.py --asin <ASIN>`。

- `build_dashboard.py`
  - 负责生成 `web/index.html`。

- `web/index.html`
  - 主要查看页面。

- `config/autodiscovery_rules.json`
  - 选品扫描规则。

- `config/category_shape_validation_rules.json`
  - 类目/形态验证规则。

- `archive/`
  - 历史机会和单品研究应尽量归档到这里或类似归档目录。

## 后续开发优先级

优先级从高到低：

1. 保证逻辑正确
2. 保证机会池不混入明显不适合产品
3. 保证历史机会和研究结果可追踪
4. 保证网页可读性
5. 再优化评分细节和视觉体验

当前最重要的方向：

- 把“单个 ASIN 评分”升级成“类目/形态机会判断”
- 把机会池做成可长期积累的档案
- 把单品研究做成可复用的完整研究页
- 每周更新但不覆盖旧结果

## 每次开始工作建议

每次重新开始时，先做这些事：

1. 阅读本文件。
2. 检查当前目录文件结构。
3. 检查现有配置规则。
4. 检查最近一次生成的数据和网页。
5. 明确这次任务属于：
   - 修 bug
   - 调筛选规则
   - 优化网页
   - 跑每周更新
   - 做单品研究
   - 解释逻辑
6. 修改前先确认影响范围。
7. 修改后尽量运行相关脚本验证。
8. 如果改了网页，重新生成 `web/index.html` 并检查展示是否清晰。
